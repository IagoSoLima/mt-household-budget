import { formatToBRLocale } from './format-to-br-locale';
import { format } from './format.date.util';
import { parseISO } from './parse-iso.date.util';

const DateUtil = {
  format,
  parseISO,
  formatToBRLocale
};

export { DateUtil };
