<style type='text/css'>
  * { margin: 0; padding: 0; box-sizing: border-box; } html, body { width: 100%;
  height: 100%; font-family: sans-serif; } main { padding: 10px 50px; } main h1
  { font-size: 20px; line-height: 20px; font-weight: 600; margin-bottom: 10px; }
  main p { font-size: 14px; line-height: 16px; font-weight: 400; margin-bottom:
  10px; margin-bottom: 50px; } main .data .month { margin-bottom: 50px;
  text-transform: capitalize; } table { width: 100%; border-collapse: collapse;
  text-align: left; border-spacing: 15px; font-size: 12px; margin-top: 20px; }
  table, td { padding: 5px 0px; } table tr { border-bottom: 0.5px solid #919191;
  } table tr td:first-child { min-width: 370px; } table tr td:not(:first-child)
  { width: 200px; } table thead tr td { color: #707070; font-weight: 600; }
</style>

<main>
  <h1>Suas Despesas</h1>
  <p>Abaixo está a lista com todas as suas despesas:</p>

  <div class='data'>
    {{#each data as |monthData|}}
      <div class='month'>
        <strong>{{month}}/{{year}}</strong>
        <table>
          <thead>
            <tr>
              <th>Descrição</th>
              <th>Data da Compra</th>
              <th>Valor</th>
            </tr>
          </thead>
          <tbody>
            {{#each monthData.data as |data|}}
              <tr>
                <td>{{data.description}}</td>
                <td>{{data.date}}</td>
                <td>{{data.amount}}</td>
              </tr>
            {{/each}}
          </tbody>
        </table>
      </div>
    {{/each}}
  </div>
</main>
